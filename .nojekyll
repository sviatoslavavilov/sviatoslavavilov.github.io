/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // You might also want this to ensure pages render as /path/index.html
  trailingSlash: true, 
};
module.exports = nextConfig;

